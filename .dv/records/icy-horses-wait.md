---
type: fix
packages:
  - '@dv-cli/dv'
notes: >-
  The exported main(argv) entrypoint now carries TSDoc (summary, @param, @returns, @example),
  raising the JSR documented-symbols score.
---

Document the dv programmatic entrypoint with JSDoc for JSR
