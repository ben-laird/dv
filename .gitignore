# Deno
node_modules/

# Compiled CLI binaries
/dist/

# VitePress build artifacts
apps/docs/.vitepress/cache/
apps/docs/.vitepress/dist/

# Editor / OS
.DS_Store

# Note: dv's in-progress record edit files (.changelog/.dv-record-edit-*)
# are ignored via the directory-local .changelog/.gitignore that
# `dv init` writes — kept there so dv owns its own directory and
# doesn't reach out to the repo root.

# Local planning scratch (per-session todo/lessons), not a project artifact
/tasks/
